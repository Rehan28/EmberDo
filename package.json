{
  "name": "ember-discipline-tracker",
  "private": true,
  "version": "1.0.0",
  "description": "Ember - Discipline Tracker. Full-stack MERN app (React + Node/Express + MongoDB).",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n API,WEB -c green,cyan \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start:backend": "npm start --prefix backend",
    "build:frontend": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
